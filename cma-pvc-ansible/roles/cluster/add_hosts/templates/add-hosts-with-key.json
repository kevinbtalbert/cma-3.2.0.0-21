{
  "hostNames" : [ {% for host in groups['pvc_cluster'] %}"{{ host }}"{% if not loop.last %},{% endif %}{% endfor %} ],
  "sshPort" : 22,
  "userName" : "{{ ansible_user }}",
  "privateKey": "{{ node_key_one_line }}",
  "javaInstallStrategy" : "NONE",
  "unlimitedJCE" : false,
  "cmRepoUrl": "{{ cm_repo_url }}"
}